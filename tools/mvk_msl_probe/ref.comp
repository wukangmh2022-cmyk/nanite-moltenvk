#version 450
layout(local_size_x = 64) in;
layout(std430, set = 0, binding = 0) buffer Dst { uint values[]; } dst;
void main() {
    atomicMax(dst.values[0], gl_GlobalInvocationID.x);
    atomicMax(dst.values[2], (gl_GlobalInvocationID.x * 2654435761u) >> 8);
}
